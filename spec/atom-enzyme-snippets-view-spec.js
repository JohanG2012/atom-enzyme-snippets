'use babel';

import AtomEnzymeSnippetsView from '../lib/atom-enzyme-snippets-view';

describe('AtomEnzymeSnippetsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
